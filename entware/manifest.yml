version: 1
name: entware
description: "Entware is a software repository for embedded devices which use the Linux kernel, primarily routers and network attached storages"
all:
  install-script: "install.sh"
  copy: "all"
available-for:
  epyc7002-5.10.55:
